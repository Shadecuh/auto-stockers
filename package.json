{
  "name": "auto-stockers",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:init": "wrangler d1 execute auto-stockers-db --remote --file=./schema.sql",
    "db:init:local": "wrangler d1 execute auto-stockers-db --local --file=./schema.sql"
  },
  "devDependencies": {
    "wrangler": "^4.0.0",
    "typescript": "^5.5.0",
    "@cloudflare/workers-types": "latest"
  }
}
